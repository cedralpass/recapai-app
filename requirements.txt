Flask==3.1.2  #flask development framework
flask-sqlalchemy==3.1.1
flask-migrate==4.1.0
psycopg2-binary==2.9.9 # driver to postgres
environs==10.3.0 #utility to load and validate environment variables
marshmallow==3.21.0 #required by environs 10.3.0 (needs >=3.13.0 for __version_info__)
redis==5.0.3
rq==1.16.2  # 1.16.1 has a bug with worker-pool --job-class parameter 
httpx==0.27.0 #making async calls to services
openai==1.14.2 #openai library to call openai
readability-lxml==0.8.1 #extract main article text from HTML for classification
lxml>=5.1.0,<5.2 #required by readability-lxml; 5.2+ moved html.clean to lxml_html_clean
flask-login==0.6.3
flask-wtf==1.2.2
wtforms<3.2  # Pin to <3.2 to avoid breaking change with flask-wtf 1.2.2 (field_flags changed from dict to tuple in WTForms 3.2.0+)
email-validator==2.1.1
flask-mail==0.10.0 #flask-mail 0.9.1 is not compatible with Flask 3.1.2
flask-cors==4.0.1 #CORS support for the /api/v1/ blueprint (Chrome extension)
pyjwt==2.8.0

# Testing dependencies
pytest==8.0.2
pytest-cov==4.1.0
pytest-mock==3.12.0
respx==0.21.1  # httpx-native HTTP mock for integration tests